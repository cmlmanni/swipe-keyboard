export const azureConfig = {
  apiKey: "", // Will be filled in later
  endpoint: "", // Will be filled in later
  deploymentId: "",
  maxTokens: 50,
  temperature: 0.3,
  useContext: true,
  contextWindowSize: 10,
};
